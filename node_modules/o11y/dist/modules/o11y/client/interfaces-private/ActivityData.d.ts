import { SchematizedData } from 'o11y/shared';
import { SchematizedPayload } from '../interfaces/SchematizedPayload';
export interface ActivityData extends SchematizedData {
    id: string;
    name: string;
    duration: number;
    userPayload?: SchematizedPayload;
    stopReason?: string;
    errorCount: number;
    isRoot?: boolean;
    preRootId?: string;
    isSampled?: boolean;
}
