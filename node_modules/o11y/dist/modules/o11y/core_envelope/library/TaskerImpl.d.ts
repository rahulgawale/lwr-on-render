import { TaskerMulti, TaskerSingle } from '../interfaces/IdleDetector';
export declare type TaskDoneNotifier = () => void;
export declare class TaskerImpl implements TaskerSingle, TaskerMulti {
    readonly name: string;
    private readonly _doneCallback;
    private readonly _overDoneCallback;
    private _busyCount;
    private _isOk;
    get isBusy(): boolean;
    constructor(name: string, initialCount: number, _doneCallback: TaskDoneNotifier, _overDoneCallback: TaskDoneNotifier);
    add(): void;
    done(): void;
}
