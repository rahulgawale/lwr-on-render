export declare class MockEvent {
    readonly type: string;
    readonly composed: boolean;
    private readonly _composedPaths;
    constructor(type: string, composed: boolean, _composedPaths: unknown[]);
    composedPath(): unknown[];
}
