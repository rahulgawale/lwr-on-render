import { Instrumentation } from '../interfaces';
export declare enum WebVitalsMetrics {
    CLS = 0,
    FCP = 1,
    FID = 2,
    LCP = 3,
    TBT = 4,
    TTFB = 5
}
declare class WebVitals {
    private _safety;
    private _instr;
    private _isInitialized;
    constructor();
    activate(instr: Instrumentation, metrics?: WebVitalsMetrics[]): void;
    private _metricsHandler;
}
export declare const webVitals: WebVitals;
export {};
