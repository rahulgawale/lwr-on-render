import { Type } from 'protobufjs';
import { SchematizedData } from 'o11y/shared';
import { LogMeta, Schema, SchematizedPayload } from '../interfaces';
import { EncodedSchematizedPayload, LogMessage, LogMessageRaw } from '../core_envelope/interfaces';
declare class ProtobufUtil {
    private readonly _typeStore;
    getSchemaType(schema: Schema): Type;
    validate(schema: Schema, data: SchematizedData): Type;
    encode(schema: Schema, data: SchematizedData): Uint8Array;
    encodePayload(rawPayload: SchematizedPayload): EncodedSchematizedPayload;
    getLogMessage(schema: Schema, data: SchematizedData, logMeta: LogMeta): LogMessage;
    processRawLogMessage(schema: Schema, raw: LogMessageRaw): LogMessage;
}
export declare const protobufUtil: ProtobufUtil;
export {};
