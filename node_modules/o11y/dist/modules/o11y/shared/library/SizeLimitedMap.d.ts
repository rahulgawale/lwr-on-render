export declare class SizeLimitedMap<K, V> {
    private readonly _map;
    private _maxSize;
    constructor(maxSize: number);
    get maxSize(): number;
    get(key: K): V;
    has(key: K): boolean;
    set(key: K, value: V): boolean;
    getElements(): V[];
    clear(): void;
}
