/**
 * Core does not support multiple module versions.  When packaging static resources like the loader shim we need to make sure
 * we replace all versioned lwr modules with version 0_0_1 which core sets all module versions as.
 *
 * @parm script The
 */
export declare function replaceModuleVersionsForCore(script: string): string;
//# sourceMappingURL=package-utils.d.ts.map