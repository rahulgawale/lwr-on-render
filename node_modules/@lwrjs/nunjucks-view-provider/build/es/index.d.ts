import { ViewProvider, ProviderContext, LwrRoute, CompiledView, ViewIdentity } from '@lwrjs/types';
export default class NunJucksViewProvider implements ViewProvider {
    private emitter;
    private watcher?;
    private watcherViewMap;
    name: string;
    routes: LwrRoute[];
    contentDir: string;
    layoutsDir: string;
    rootDir: string;
    constructor(pluginConfig: unknown, providerConfig: ProviderContext);
    onViewChange(fileChanged: string): Promise<void>;
    initialize(): Promise<void>;
    private watchView;
    getView(viewId: ViewIdentity): Promise<CompiledView | undefined>;
}
//# sourceMappingURL=index.d.ts.map