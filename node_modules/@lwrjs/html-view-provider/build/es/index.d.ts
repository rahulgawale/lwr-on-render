import { ViewProvider, ProviderContext, CompiledView, ViewIdentity, Watcher } from '@lwrjs/types';
export default class HtmlViewProvider implements ViewProvider {
    name: string;
    contentDir: string;
    layoutsDir: string;
    watcher?: Watcher;
    private emitter;
    private cachedViews;
    constructor(pluginConfig: unknown, providerConfig: ProviderContext);
    onModuleChange(fileChanged: string): Promise<void>;
    initialize(): Promise<void>;
    getView(viewId: ViewIdentity): Promise<CompiledView | undefined>;
}
//# sourceMappingURL=index.d.ts.map