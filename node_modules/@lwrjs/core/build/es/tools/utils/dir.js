import { join } from 'path';
import fs from 'fs';
export const skipDirCreation = '__skip_directory_creation__';
export function createResourceDir(resourcePath, parentDir) {
    const dir = join(parentDir, resourcePath);
    createDir(dir);
    return dir;
}
export function createDir(dir) {
    if (dir && dir.indexOf(skipDirCreation) === -1) {
        if (!fs.existsSync(dir)) {
            fs.mkdirSync(dir, { recursive: true });
        }
    }
}
//# sourceMappingURL=dir.js.map