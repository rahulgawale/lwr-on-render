import { PluggableList } from 'unified';
import { AssetReference } from '@lwrjs/types';
import { VFile } from 'vfile';
export interface MarkdownParser {
    render(vFile: VFile): Promise<MarkdownResult>;
}
export interface HeadingMetadata {
    depth: 1 | 2 | 3 | 4 | 5 | 6;
    text: string;
    slug: string;
}
export interface MarkdownMetadata {
    headings: HeadingMetadata[];
    customElements: string[];
    images: AssetReference[];
}
export interface MarkdownResult {
    html: string;
    metadata: MarkdownMetadata;
}
export interface MarkdownParserOptions {
    markdownPlugins?: PluggableList;
    htmlPlugins?: PluggableList;
    codeHighlightTheme?: string;
}
export default function markdownParser(options?: MarkdownParserOptions): Promise<MarkdownParser>;
//# sourceMappingURL=markdown-parser.d.ts.map